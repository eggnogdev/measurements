name: measurements
description: adding measurements to dart.
version: 0.1.0
repository: https://codeberg.org/eggnog/measurements

environment:
  sdk: ^3.1.5

dependencies:
  dart_numerics: ^0.0.6

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
